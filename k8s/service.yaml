apiVersion: v1
kind: Service
metadata:
  name: nuxt-app-service
  namespace: nuxt-app
  labels:
    app: nuxt-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: nuxt-app
